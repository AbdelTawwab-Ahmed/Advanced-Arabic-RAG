# Production-Ready-Arabic-RAG

Production-ready RAG system for Arabic PDF data — currently in the ingestion/indexing phase.

## Status
🚧 Work in progress — ingestion pipeline (extraction → chunking → embedding → hybrid indexing).